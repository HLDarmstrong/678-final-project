---
title: "678-final-project"
author: "Shengbo Wang"
date: "2022-11-29"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

```{r}
library(tidyr)
library(ggplot2)
library(tidyverse)
library(dplyr)
```


```{r}
players <- read_csv("players.csv") %>% select(playerId,nameFull,nflId,collegeId)

salaries <- read_csv("football_salaries.csv") %>% select(position,nameFull=player,team,avg_year)

fumblForced <- read_csv("fumblForced.csv")
fumbles <- read_csv("fumbles.csv")

finalDate <- inner_join(players,salaries,by="nameFull") %>% na.omit()
```

